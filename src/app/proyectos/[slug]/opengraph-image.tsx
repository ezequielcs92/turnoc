import { ImageResponse } from "next/og";
import { getProjectBySlug } from "@/lib/supabase/data";
export const alt = "Proyecto de Compañía Turnoc"; export const size = { width: 1200, height: 630 }; export const contentType = "image/png";
export default async function Image({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const project = await getProjectBySlug(slug); return new ImageResponse(<div style={{ width: "100%", height: "100%", display: "flex", flexDirection: "column", justifyContent: "space-between", padding: 72, background: "#ff5b46", color: "#120f10" }}><div style={{ fontSize: 28 }}>COMPAÑÍA TURNOC · PROYECTO</div><div style={{ fontFamily: "serif", fontSize: 96, lineHeight: .92 }}>{project?.title ?? "Proyecto"}</div><div style={{ fontSize: 24 }}>Archivo · escena · comunidad</div></div>, size); }
